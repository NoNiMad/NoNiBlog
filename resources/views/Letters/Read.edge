<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8" />
    <title>{{ letter.title }}</title>
    {{ style('bulma.min') }}
    {{ style('style_letter') }}
</head>

<body>
    <div class="container">
        <div class="columns">
            <div class="column">
                <div class="box">
                    <div class="has-text-centered title is-2">{{ letter.title }}</div>
                    <div class="subtitle is-6"><i>Pour {{ letter.recipient }}, de la part de {{ letter.author.username }}</i></div>
                    <div class="content markdown">
                        {{{ letter.content }}}
                    </div>
                    <div id="date">Écrite le {{ letter.updated_at }}</div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>