<div class="box">
  <div class="title is-4">{{ post.title }}</div>
  <div class="subtitle is-6 is-pulled-right">le {{ post.created_at }} par {{ post.author.username }}</div>
  <div class="content">
    {{ post.content }}
  </div>
</div>